# 2026-09-18

## 审查 tester agent 的浏览器自动化工具选型（行业现状核对）

核对对象：`agents/tester.md` → 路由到 `skills/browser-automation`（v2.2.0）三条通道。

结论：选型方向正确，Playwright 是 2026 年行业默认首选（新项目占比与下载量均压过 Selenium/Cypress）。

发现的问题（待用户确认后再改）：
- `playwright/SKILL.md` 与 `browser-automation/SKILL.md` 写 `@playwright/cli` 最后版本 0.1.18，实际 npm latest 为 0.1.20（建议改成"已废弃"不写死版本号）。
- `agent-browser` 本机 0.27.0，npm latest 0.38.1（落后 11 个版本）。
- Windows 环境不支持 agent-browser，但路由表把它排在第 1 优先级，本机命中即不可用。
- Playwright npm latest 1.63.0（技能里 min 1.62.0 作为下限没问题）。

缺口建议：视觉回归基线比对、axe-core 无障碍测试、真机/移动端（Appium）、MCP 形态（Chrome DevTools MCP / Playwright MCP）未在配置中体现。

## 已按上述结论落地修改（用户确认"按最优方案修改"）

改动文件（`.ai-shared` 为唯一维护源，`skills\`/`agents\` 均以 Junction 分发，改源即全生效）：

1. `skills/browser-automation/SKILL.md`
   - metadata 升到 v2.3.0，新增 `platform_note`
   - 判定顺序第 1 条加 Windows 平台限制与降级路径（探索性测试 → Playwright 人工循环；Electron → 报环境阻塞）
   - 切换信号表新增「已切到 Agent Browser 但本机是 Windows」回退行
   - agent-browser 安装改为 `@latest`，删除写死的 0.27.0，改为不写死版本号
   - 通用注意事项第 1 条加平台预检
2. `skills/browser-automation/playwright/SKILL.md`
   - 删除 `@playwright/cli` 最后版本 0.1.18 的错误表述，改为「已废弃，勿安装勿引用，不跟踪其版本号」
3. `agents/tester.md`
   - 分层测试策略新增三行：视觉回归 / 无障碍（a11y，axe-core）/ 真机移动端（Appium、设备模拟≠真机）
   - 浏览器 E2E 执行规范新增第 8 条（平台与通道降级）、第 9 条（MCP 通道复用会话但留证要求不变）
   - 证据留存条目补入视觉回归差异截图

未做：agent-browser 二进制未升级（0.27.0 → 0.38.1），因 Windows 不支持该工具，升级无收益，且涉及全局 npm 安装需用户确认。

## 第二轮：删除 Agent Browser 通道（用户确认"不能用直接删除"）

- `skills/browser-automation/agent-browser/` 整个目录移入 `.trash/browser-automation-agent-browser-20260918/`（该文件 git 已跟踪，`.trash/` 已被 .gitignore 忽略，双重可恢复）
- `skills/browser-automation/SKILL.md` 重写为 v2.4.0：两条通道（Webapp Testing / Playwright）；frontmatter 删掉 Electron/桌面应用/探索性测试/dogfood 触发词与 platform_note；新增「不覆盖范围」（Electron 桌面应用、真机系统级交互 → 命中即报环境阻塞）；顺带修掉正文里残留的 `@playwright/cli` 0.1.18 旧表述
- `agents/tester.md` E2E 规范第 8 条由「平台与通道降级」改为「能力边界」
- `skills/README.md` 花名册第 5 条描述同步去掉 Electron 桌面探索
- 已复查：`.ai-shared` 内无指向已删通道的悬空引用（仅保留一条说明性提及）

约定沉淀：本环境（Windows）浏览器自动化只走 Web 两条通道；Electron/桌面应用、真机一律按环境阻塞上报。

## 第三轮：撤掉「真机 / 移动端」测试层次

用户决定不引入真机测试（需真机或云真机账户，成本高）。已从 `agents/tester.md` 分层测试策略删除「真机 / 移动端」行，只保留视觉回归与 a11y（纯软件可跑）。
保留项：E2E 规范第 8 条「能力边界」中的真机表述不动——那是能力边界声明（防止 agent 误以为能做真机），不是待执行的测试层次。
用户偏好：测试体系只纳入本机纯软件即可执行的层次，不引外部依赖/账户型测试。
