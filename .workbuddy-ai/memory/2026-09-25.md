# 2026-09-25

## skills 收敛：fireworks 两个合并为一个（用户裁决：合并进引擎）

- `fireworks-showcase-layout` 不是独立技能，是 `fireworks-tech-graph` 的高级用法补遗
  （其 description 首句即「用 fireworks-tech-graph 画…时」）。已合并：
  - `SKILL.md` → 引擎 `references/showcase-layout.md`；`fwg_layout.py`（543 行版式模块）→ `references/`
  - 引擎 SKILL.md 新增「Hand-placed coordinates and showcase grade」节 + description 触发词
  - 原目录移入 `.trash/fireworks-showcase-layout`（先全量校验再删源）
  - 提交 e9934fb
- **坑**：新增的两个文件落在 `.gitignore` 覆盖的克隆目录内（`skills/fireworks-tech-graph/` 整体被忽略），
  对克隆自身 git 又是 untracked ⇒ **双重不受版本控制**。若将来在克隆里 `git clean -fd` 会直接丢。
  要固化只能在克隆仓库里显式 `git add`（需用户下令）。

## fireworks-tech-graph 内嵌副本：备份已做，删除被拦

- 内嵌路径 `skills/fireworks-tech-graph/skills/fireworks-tech-graph/`，140 文件 / 9.0MB，
  与本体**逐字节相同**，唯一差异是 `scripts/generate-from-template.py`（内嵌是上游原版，
  **没有本地 `body` 多行扩展**）⇒ 保留本体、删内嵌没有悬念。
- 成因：上游把「安装态目录」也提交了；克隆时自然带进来。
- 已全量 md5 校验后备份到 `.trash/fireworks-tech-graph-nested-copy`（140 文件 / 9.0M）。
- **删除源目录失败**：本机 `shutil.rmtree` 走 trash 时报错（Some operations were aborted），
  PowerShell `Move-Item` 也失败，用户又取消了批量删除授权 ⇒ 只能由用户手动删：
  `rm -rf "C:/Users/20448/.ai-shared/skills/fireworks-tech-graph/skills"`
  删完后克隆 `git status` 会永久显示 140 个 deleted（该副本在 HEAD 中跟踪），
  想在克隆里 commit 掉需用户下令。

## 用户手动删除 19 个 Codex 生态 skill，已提交

- 删除整目录：automate / babysit / create-hook / create-rule / create-skill / create-subagent /
  framework-migration-verification / loop / plan-premise-verification / project-handover-docs /
  review / review-bugbot / review-security / sdk / shell / split-to-prs / statusline /
  update-cli-config / update-cursor-settings
- README 索引同步：删 19 行 + 重编号，总数 60 → 59 → **40**；「已知功能重叠」段删掉已失效的
  create-* 四件套说明。校验脚本确认索引 ↔ 磁盘双向一致（无悬空、无遗漏）。
- 提交 906e2e8（未 push）。工作区干净。

**可复用做法**：改完 skills 索引后跑一次双向校验（索引指向的目录存在 + 磁盘目录都在索引里），
比肉眼核对可靠；本次就是靠它确认 40 项无悬空。
