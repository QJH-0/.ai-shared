# 2026-09-24

## 规范修订：反堆砌 / 合并产物去注释 / 放开子代理派发限制

提交：`5483074 feat(governance): 反堆砌规范、合并产物去注释、放开子代理派发限制`

- **AGENTS.md**：新增「编码与注释规范 → 反堆砌」（代码四条禁止：新旧并排 / 包装规避 / 重复防御 / 预留式抽象；注释三条：只减不增 / 禁止段落式 / 过程信息不入码；外加格式前提）；「全局禁止项」加一条；「代码输出自检」扩为三项（注释合规 / 无堆砌 / 格式合法）。
- **agents/coder.md**：工作规则新增「不堆砌」（原规则 7、8 顺延为 8、9）；DoD 与输出格式改为「注释与堆砌自检」。
- **agents/standards/lifecycle-gates.md**：出口门禁的注释自检项同步为「注释与堆砌自检」。
- **skills/git-branch-diff-merge/SKILL.md**：删除把 `[合并处理]` 标记写进源码的 3 处规定（Step 3 第三步、Step 4 注释模板、红线 4 的「注释说明」括注），融合产物改为可直接提交的干净代码，取舍依据统一由报告「① 冲突与合并分析」承载；红线 6 条 → 7 条。
- **子代理派发**：AGENTS.md「委派边界」改写为「派发规范」——任何角色均可派发子代理；保留门禁与验证结论不可委派、写入结论须复核原始证据、简报四要素、成本边界、并行边界。analyst / coder / researcher / reviewer / sre / tester 六个角色文件同步清理禁止派发表述。
- **派生副本**：`agents/*.toml` 按各自格式从对应 `.md` 重生成（详见 MEMORY.md），顺带修正 analyst / researcher 中 `Codex-deep-research-skill` 的悬空引用。

## 另提交

`ed40599 chore(skills): 21 个 skill 入库并同步花名册与链接清单` —— 工作区既有改动（21 个未跟踪 skill 目录、花名册、链接清单、.gitignore）。

## 待办 / 发现

- `skills/ai-config-sharing/SKILL.md` 未记录 `agents/*.md → *.toml` 的派生关系（该 skill 非 `agent_created`，本次未改），建议补入「修改与分发」章节。
- 6 组 md/toml 仍靠手工同步，无生成脚本——「单一维护源」的既有缺口，可考虑加一个同步脚本。

## 后续处理（同日晚）

上面两条已结清：

- 新增 `skills/ai-config-sharing/scripts/sync-agent-toml.py`，从 `agents/*.md` 重生成派生副本 `agents/*.toml`，支持 `--check`（漂移则退出码 1）。验证：对现有 6 个 toml **字节级复现**（写回后 `git status` 为空）；反向验证能检出并修复故意制造的 2 处漂移，且方向正确（toml 以 md 为准）。
- `skills/ai-config-sharing/SKILL.md` 新增「日常维护 4：agents 角色定义：改 `.md` 后必须同步 `.toml`」，并在「完整脚本」表、`### 维护` 与 frontmatter 触发词中补入指针。该 skill 非 `agent_created`，直接编辑而非经 SkillManage。
- 踩坑：脚本里 `developer_instructions = """` 后**不能换行**，内容要紧跟起始引号；末行仍带字面量 `\r`，收尾引号紧随其后。TOML 语义上换行会被裁剪，所以两种写法等价，但字节级复现必须严格对齐。

## 新增 skill 与花名册对账

提交：`63faa5a feat(skills): 新增 ai-fast-learning-loop，并补齐花名册 3 个漏登项`

- `skills/ai-fast-learning-loop/` 入库（SKILL.md + references/prompt-templates.md + references/worked-example.md + agents/openai.yaml）。
- 花名册对账发现：实测 60 个 skill 目录，`skills/README.md` 只有 57 条，缺 `ai-fast-learning-loop`、`fireworks-showcase-layout`、`project-handover-docs` 三项——后两项是我在 `ed40599` 里入库目录时漏登记的。已全部补登记，总数 57 → 60，来源分布「主仓库跟踪」50 → 53，类别分布同步，全表按字典序重编号。
- `HARDLINK_INVENTORY.md` §四 实测数与复核命令期望值改为 60 / 61，来源分布改为 7 + 53，恢复历史追加 2026-09-24 行。
- 教训：**入库新 skill 目录时必须同步登记花名册**，否则「实测目录 vs 花名册」立刻漂移；花名册的 `#` 列是连续编号，插入后需整表重编号（本次用脚本按字典序重排后重编号，并加断言校验原表本身是字典序）。

## skills 审查 + 禁止自行创建 skill 规则

产出：`.agent_docs/audits/2026-09-24-skills-audit.md`（只做识别，未删改任何文件）

审查方法（可复用）：① frontmatter `name:` 全库重名扫描 ② 目录名 vs `name` 一致性 ③ 描述聚类找功能重叠 ④ **逐组读正文确认** ⑤ 悬空引用扫描 ⑥ 体积分布。第 ④ 步是关键——只看描述会误判，本次至少 4 组「看着像重复」实为设计（见下）。

结论：
- **唯一确认的重复**：`skills/fireworks-tech-graph/skills/fireworks-tech-graph/`，与父目录 190 文件中的 140 个重复、`SKILL.md` md5 相同、占 9.0MB。上游仓库的发布布局，处置有 A/B/C 三方案（推荐 A：文档化豁免，因 `git pull` 会拉回）。
- **高度重叠**：`create-skill`（504 行）能力是 `skill-creator`（485 行，含 eval）的真子集，无外部引用；`officecli` 基座与 `officecli-docx/xlsx/pptx` 对同一 `.docx` 任务双向命中，且基座正文不引用格式分支（不是路由器）。
- **触发边界冲突 4 组**：`web-access`↔`browser-automation`（前者声称「所有联网操作必须通过此 skill」）、`officecli`↔`officecli-*`、`ai-code-review`↔`review*`、`loop`↔`automate`。
- **命名缺陷**：`claude-deep-research-skill` 目录名与 `name: deep-research` 不一致。
- **误判排除（不要动）**：`review` 是 16 行斜杠命令菜单（`disable-model-invocation: true`）；`frontend-design` 顶层是容器索引、嵌套才是真身；`morph-ppt-3d` 是显式 extends；`nature-skills` 是上游合集。
- **悬空引用 0 个**（启发式扫描的命中全是 CSS 属性、CLI 名、示例名等误报）。

规则改动（`AGENTS.md`）：
- 「全局禁止项」新增：禁止自行创建 skill——任务收尾**主动询问用户**，获同意才创建。
- 「元规则 → 升级」改为指向该禁止项。
- 「交付说明要求」新增第 6 项「是否沉淀为 skill」，把询问钉进交付清单。
- 复核：`AGENTS.md` 硬链接数仍为 9。

## 审查方法上的坑

- 用正则解析 frontmatter 的 `description` 时，`\s*` 贪婪会吃掉整段导致 `(.*?)` 匹配空；改用**按行解析 + 手动处理块标量**才可靠。
- `web-access` 的 description 用普通多行标量（无 `>-`），按 `>-` 处理的解析器会误判为「空描述」——实为合法 YAML。
- 9 个 skill（`morph-ppt` 与 8 个 `officecli-*`）的 description 用双引号包裹，已核对引号配对，合法，仅风格不统一。

## 处置执行（零风险项）

提交：`47a7c6b docs(skills): 划清 5 组 skill 的触发边界，并登记已知重名与命名不一致`

- 5 处 description 加边界句：`browser-automation`（搜索/登录态抓取/社交媒体归 `web-access`）、`officecli`（收窄为 CLI 工具本身）、`ai-code-review`（只提供方法论，子代理入口走 `review*`）、`loop`（仅会话内循环）、`code-to-interview-docs`（输入源限定）。
- `skills/README.md` 新增「已知重名与命名不一致（加载说明）」+「已知功能重叠（保留，未合并）」，把 4 条预期状态登记在案，下次审查免重复排查。
- 审查报告追加「九、处置进度」，标注已执行 / 未动项与取舍理由。

**关键设计取舍（可复用）**：`web-access` 与 `claude-deep-research-skill` 是**干净克隆**（本地改动 0 项），直接改其 `SKILL.md` 会让 `git pull --ff-only` 失败。因此**边界改在主仓库侧承担、命名以目录名为准**，两条都文档化。以后遇到「第三方克隆 skill 需要改」时，优先看能否把改动落到主仓库侧的对端 skill 上。

> 另注：本仓 7 个克隆中，`repo-wiki`(7)、`fireworks-tech-graph`(2)、`superpowers`(1)、`nature-skills`(1) **已有本地改动**，即「保持克隆干净」并非本仓现状；只有 `web-access`、`claude-deep-research-skill`、`humanizer-zh` 是干净的。

## 克隆改动溯源（重要发现）

追查那 4 个克隆的改动到底是什么，结论写进了 `MEMORY.md`「GitHub 克隆 skill 的本地改动状态」：

- `superpowers` / `nature-skills`：只是**新增根 `SKILL.md`**（上游顶层无此文件，安全，不与 pull 冲突）。
- `repo-wiki`：**展平**——删了上游跟踪的 `repo-wiki/SKILL.md`，把内容提到根目录。上游更新该路径会冲突。
- `fireworks-tech-graph`：**本地功能扩展**——给 rect 节点加了多行 `body: [line, ...]`，CHANGELOG 里自标 `Local extension — 2026-09-23 (not upstream)`，2 文件 / +31 行。**这不是噪声，是有意的工作成果，被 `git pull` 覆盖会直接丢。**

由此得出两条结论：
1. `skills/README.md`「更新方法」写「7 个克隆都 `git pull --ff-only`」**是错的**，对 4 个克隆会失败或覆盖成果，需按克隆分类改写。
2. 以后遇到「第三方克隆 skill 需要改」，**优先把改动落到主仓库侧的对端 skill**（`web-access` 的边界就是这么处理的），而不是改克隆。

审查报告已追加「十、待决策清单与推荐」，覆盖全部 16 项遗留问题（含推荐项与理由），等用户裁决。

## 决策执行（全部采纳推荐项）

提交：`eb63a5a docs(skills): 执行决策清单——create-skill 划边界、修正克隆更新指令`

16 项中处理 5 项，其余 11 项判定无需动作。**未删除或移动任何文件**：

- #2 `create-skill`：description 加边界句（只讲 SKILL.md 结构；创建/修改/评测走 `skill-creator`）
- #11 `skills/README.md`「更新方法」**按克隆分三类改写**——原文「7 个克隆都 `git pull --ff-only`」是错的
- #8 登记 `repo-wiki` 的展平改动
- #9 登记 `fireworks-tech-graph` 的本地功能扩展（`body` 多行节点 / 改动文件 / 动机 / `NODE_LINE_HEIGHT` 契约），标注「被 pull 覆盖会直接丢失」
- #1 `fireworks-tech-graph` 内嵌副本改判为「预期状态，不动」（副本是上游跟踪内容）

审查报告 §十 标注决策结果，§十一 记决策执行记录。

**用户偏好**：对同类「多问题待裁决」场景，倾向一次性拿到**全量清单 + 每项推荐 + 理由**，再统一裁决，而不是逐项来回问。
