# 2026-09-24

## 规范修订：反堆砌 / 合并产物去注释 / 放开子代理派发限制

提交：`5483074 feat(governance): 反堆砌规范、合并产物去注释、放开子代理派发限制`

- **AGENTS.md**：新增「编码与注释规范 → 反堆砌」（代码四条禁止：新旧并排 / 包装规避 / 重复防御 / 预留式抽象；注释三条：只减不增 / 禁止段落式 / 过程信息不入码；外加格式前提）；「全局禁止项」加一条；「代码输出自检」扩为三项（注释合规 / 无堆砌 / 格式合法）。
- **agents/coder.md**：工作规则新增「不堆砌」（原规则 7、8 顺延为 8、9）；DoD 与输出格式改为「注释与堆砌自检」。
- **agents/standards/lifecycle-gates.md**：出口门禁的注释自检项同步为「注释与堆砌自检」。
- **skills/git-branch-diff-merge/SKILL.md**：删除把 `[合并处理]` 标记写进源码的 3 处规定（Step 3 第三步、Step 4 注释模板、红线 4 的「注释说明」括注），融合产物改为可直接提交的干净代码，取舍依据统一由报告「① 冲突与合并分析」承载；红线 6 条 → 7 条。
- **子代理派发**：AGENTS.md「委派边界」改写为「派发规范」——任何角色均可派发子代理；保留门禁与验证结论不可委派、写入结论须复核原始证据、简报四要素、成本边界、并行边界。analyst / coder / researcher / reviewer / sre / tester 六个角色文件同步清理禁止派发表述。
- **派生副本**：`agents/*.toml` 按各自格式从对应 `.md` 重生成（详见 MEMORY.md），顺带修正 analyst / researcher 中 `Codex-deep-research-skill` 的悬空引用。

## 另提交

`ed40599 chore(skills): 21 个 skill 入库并同步花名册与链接清单` —— 工作区既有改动（21 个未跟踪 skill 目录、花名册、链接清单、.gitignore）。

## 待办 / 发现

- `skills/ai-config-sharing/SKILL.md` 未记录 `agents/*.md → *.toml` 的派生关系（该 skill 非 `agent_created`，本次未改），建议补入「修改与分发」章节。
- 6 组 md/toml 仍靠手工同步，无生成脚本——「单一维护源」的既有缺口，可考虑加一个同步脚本。

## 后续处理（同日晚）

上面两条已结清：

- 新增 `skills/ai-config-sharing/scripts/sync-agent-toml.py`，从 `agents/*.md` 重生成派生副本 `agents/*.toml`，支持 `--check`（漂移则退出码 1）。验证：对现有 6 个 toml **字节级复现**（写回后 `git status` 为空）；反向验证能检出并修复故意制造的 2 处漂移，且方向正确（toml 以 md 为准）。
- `skills/ai-config-sharing/SKILL.md` 新增「日常维护 4：agents 角色定义：改 `.md` 后必须同步 `.toml`」，并在「完整脚本」表、`### 维护` 与 frontmatter 触发词中补入指针。该 skill 非 `agent_created`，直接编辑而非经 SkillManage。
- 踩坑：脚本里 `developer_instructions = """` 后**不能换行**，内容要紧跟起始引号；末行仍带字面量 `\r`，收尾引号紧随其后。TOML 语义上换行会被裁剪，所以两种写法等价，但字节级复现必须严格对齐。

## 新增 skill 与花名册对账

提交：`63faa5a feat(skills): 新增 ai-fast-learning-loop，并补齐花名册 3 个漏登项`

- `skills/ai-fast-learning-loop/` 入库（SKILL.md + references/prompt-templates.md + references/worked-example.md + agents/openai.yaml）。
- 花名册对账发现：实测 60 个 skill 目录，`skills/README.md` 只有 57 条，缺 `ai-fast-learning-loop`、`fireworks-showcase-layout`、`project-handover-docs` 三项——后两项是我在 `ed40599` 里入库目录时漏登记的。已全部补登记，总数 57 → 60，来源分布「主仓库跟踪」50 → 53，类别分布同步，全表按字典序重编号。
- `HARDLINK_INVENTORY.md` §四 实测数与复核命令期望值改为 60 / 61，来源分布改为 7 + 53，恢复历史追加 2026-09-24 行。
- 教训：**入库新 skill 目录时必须同步登记花名册**，否则「实测目录 vs 花名册」立刻漂移；花名册的 `#` 列是连续编号，插入后需整表重编号（本次用脚本按字典序重排后重编号，并加断言校验原表本身是字典序）。
