# 2026-09-19

## SpEx+ 训练日志按运行拆分（D:\Code\00cursor_code\ConvtasnetTwo）

- 单源在 `spex/config.py`，两个 notebook（Kaggle 版 / 服务器版）由 `build_spex_nb.py` 生成，
  改完必须跑 `python build_spex_nb.py` 重建，不要手改 notebook。
- 改动：`LOG_FILE` 由 `EXP_DIR/training.log` 改为 `EXP_DIR/logs/training_<RUN_ID>.log`
  （RUN_ID=`%Y%m%d_%H%M%S`，同秒撞名加 `-1` 后缀），日志头多打一行「本次运行 ID」。
- 旧的根目录 `training.log`（8 次运行、2855 行、来自 /root/rivermind-data 服务器）已按
  `[log] 日志已开启` 边界切到 `ConvtasnetTwo/logs/training_run01..08.log`，合并回放校验无损。
  切分陷阱：`import_resume_checkpoints()` 的 `Imported N checkpoint(s) from ...` 打印在日志头
  **之前**，属于紧随其后的那一次运行，按 marker 硬切会把它留在上一段尾部。

## run08 日志分析结论（SpEx+ 渐进位宽）

- 各阶段最佳 val SI-SDRi：S3(W2A32) 12.37 → S4(W1A32) 11.26 → S5(W1A8) 11.33 →
  S6(W1A4) 11.11 → S7(W1A2) 9.15 → **S8b(W1A1+KD) 0.64 / S8a(无KD) 0.59**。
  断崖只在 A2→A1，A8/A4 几乎无损，A2 只掉 2 dB。
- W1A1 崩溃形态：软 sign warmup 15ep 内单调 3.10→−0.05，硬 sign 后 35ep 只回到 0.64。
  非数值发散（loss 稳定 ~2.1），是 TCN 表征塌缩；CE 仍 0.02~0.2 说明说话人编码器（未量化）还活着。
- **发现 bug**：`spex/train.py:552-556` 只在 `should_run_stage('stage1')` 为真时构建
  fp32_teacher。续训跑到 stage≥2 时 stage1 被跳过 → 整条 wave KD 静默失效（run08 全程
  kd_wave=0.000，且不打 WARNING）。所以 run08 的 S8b vs S8a 实际比的是「仅 feat KD」vs「无 KD」，
  §4.4 的 KD 净贡献结论在该次运行中不成立。
- run04 的 FINAL RESULTS 表不能当结果：那是 SMOKE_TEST=ON + quick200(140 样本) + 每阶段 1ep。
- 量化范围：只有 TCN block 的 conv1x1(Expand) 与 sconv(Res) 被量化（79% 参数），
  激活量化也只挂在 TCN 的 Expand/Residual 两处输入；encoder/decoder/speaker encoder/pred_linear 全 FP32。
